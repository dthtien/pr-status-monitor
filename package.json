{
    "name": "pr-status-monitor",
    "version": "1.0.0",
    "description": "GitHub Action to monitor PR status and identify stalled, unassigned, or blocked pull requests",
    "main": "dist/index.js",
    "scripts": {
        "build": "ncc build src/index.js -o dist",
        "test": "jest",
        "lint": "eslint src/**/*.js",
        "package": "npm run build && npm run test"
    },
    "keywords": [
        "github",
        "action",
        "pr",
        "monitor",
        "stale",
        "blocked"
    ],
    "author": "GitHub Action Expert",
    "license": "MIT",
    "dependencies": {
        "@actions/core": "^1.10.0",
        "@actions/github": "^6.0.0",
        "axios": "^1.6.0"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.38.0",
        "eslint": "^8.0.0",
        "jest": "^29.0.0"
    }
}